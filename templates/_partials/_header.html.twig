<header>
	<nav class="navbar navbar-expand-lg">
		<div class="container-fluid">
			<a class="navbar-brand" href="{{path('app_home')}}">
				<div class="d-flex align-items-center justify-content-center">
					<div class="col-4">
						<img src="/pictures/logo.png" alt="Logo" width="200" height="auto" class="d-inline-block align-text-top">
					</div>
					<div class="col-8">
						<h2>Les Recettes de Cécilia</h2>
					</div>
				</div>
			</a>
			<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
				<span class="navbar-toggler-icon"></span>
			</button>
			<div class="collapse navbar-collapse" id="navbarRightAlignExample">
				{% if app.user is null %}
					<ul class="navbar-nav ms-auto mb-2 mb-lg-0">
						<li class="nav-item">
							<a class="nav-link active" aria-current="page" href="{{path('app_register')}}">Inscription</a>
						</li>
						<li class="nav-item">
							<a class="nav-link" href="{{path('app_login')}}">Connexion
							</a>
						</li>
					</ul>
				{% else %}
					{% if app.user.roles[0] == "ROLE_USER" %}
						<ul class="navbar-nav ms-auto mb-2 mb-lg-0">
							<li class="nav-item">
								<a class="nav-link active" aria-current="page" href="#">Mes recettes</a>
							</li>
							<li class="nav-item">
								<a class="nav-link" href="#">Mes menus</a>
							</li>
							<li class="nav-item">
								<a class="nav-link" href="{{path('app_user_show')}}">Mon compte</a>
							</li>
							<li class="nav-item">
								<a class="nav-link" href="{{ path('app_logout') }}">Déconnexion</a>
							</li>
						</ul>
					{% elseif app.user.roles[0] == "ROLE_ADMIN" %}
						<ul class="navbar-nav ms-auto mb-2 mb-lg-0">
							<li class="nav-item">
								<a class="nav-link active" aria-current="page" href="{{path('admin')}}">Tableau de Bord</a>
							</li>
							<li class="nav-item">
								<a class="nav-link" href="{{ path('app_logout') }}">Déconnexion</a>
							</li>
						</ul>
					{% endif %}
				{% endif %}
			</div>
		</div>
	</nav>
</header>
