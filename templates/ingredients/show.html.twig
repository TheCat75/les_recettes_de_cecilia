{% extends 'base.html.twig' %}

{% block title %}Ingredients{% endblock %}

{% block body %}
    <h1>Ingredients</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ingredient.id }}</td>
            </tr>
            <tr>
                <th>NameIngredient</th>
                <td>{{ ingredient.nameIngredient }}</td>
            </tr>
            <tr>
                <th>Quantities</th>
                <td>{{ ingredient.quantities }}</td>
            </tr>
            <tr>
                <th>Units</th>
                <td>{{ ingredient.units }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ ingredient.createdAt ? ingredient.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ ingredient.updatedAt ? ingredient.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_ingredients_index') }}">back to list</a>

    <a href="{{ path('app_ingredients_edit', {'id': ingredient.id}) }}">edit</a>

    {{ include('ingredients/_delete_form.html.twig') }}
{% endblock %}
